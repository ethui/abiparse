{
  "name": "@ethui/abiparse",
  "version": "0.0.4",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "bun run build.mjs",
    "prepblishOnly": "bun run build"
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chevrotain": "^11.0.3",
    "nearly": "^1.8.1",
    "viem": "^2.9.20"
  },
  "devDependencies": {
    "bun-plugin-dts": "^0.2.3",
    "bun-types": "^1.1.4",
    "vitest": "^1.5.0"
  }
}
